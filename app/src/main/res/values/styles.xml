<resources>

    <style name="Theme.PasiekaApp" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="labelVisibilityMode">labeled</item>
    </style>

    <style name="Theme.PasiekaApp.withSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/pasiekaapp_logo_splashscreen</item>
        <item name="postSplashScreenTheme">@style/Theme.PasiekaApp</item>
    </style>

</resources>